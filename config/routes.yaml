# config/routes.yaml

# Home page route should be handled by annotations/attributes in controller
# Make sure controllers are imported
controllers:
    resource: ../src/Controller/
    type: attribute

# Or if you want to explicitly define it:
# app_home:
#     path: /
#     controller: App\Controller\HomeController::index